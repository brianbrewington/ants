# Secrets — never commit (holds GITHUB_PAT and anything else local)
.env
.env.*
!.env.example

# Python
backend/.venv/
**/__pycache__/
*.pyc
*.pyo

# Node / frontend build
frontend/node_modules/
frontend/dist/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Local agent review artifacts (raw reviews + per-cycle response bookkeeping).
# Durable deferrals live in docs/ARCHITECTURE.md ("Known limitations & deferred work").
docs/codereviews/
docs/review-responses/
