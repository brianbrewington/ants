# Response to GPT-5.5 refactor-cleanliness review (2026-06-14)

Verdict from the review: the refactor "landed mostly cleanly… no functional
blocker." Findings were polish. Dispositions:

## Fixed (in this round)
| Finding | Fix |
|---|---|
| Med: `reset()` doesn't rebind the regime (fragile if cfg mutated) | `reset()` now `make_regime(self.cfg)` at the top. |
| Med: action mix is last-step only at speed>1 | `Simulation.advance()` now averages `frac_*` across the frame (alongside summed births/deaths/food). |
| Med: history mixes logistic↔nutrient (restart signal lacks `food_model`) | `useSim` clears history on `food_model` change too. |
| Med: ARCH doc overstates learner drop-in | RL section now says stateless brains use the policy seam; **stateful learners need a trainer/registry layer** before Lesson 1. |
| Med: contracts started but thin | Added `Snapshot` + `FrameMetrics` TypedDicts; annotated `to_snapshot`/`advance`. |
| Low: README module map stale | Updated to match `docs/ARCHITECTURE.md`. |

## Deferred (with reasons)
| Finding | Why deferred |
|---|---|
| Med: O(N²) comm runs at UI caps (6000→36M < cap) | Known scaling limit, not a regression. Real fix = spatial binning, scheduled before the learned-communication lesson. |
| Med: bifurcation lacks explicit nutrient/logistic sweep contract | Works (inherits live `food_model`); an explicit preset+test is a small later add. |
| Med: backend suite not runnable in reviewer's env (no venv/torch) | Local env limitation; suite is 38/38 green in ours. Real fix = **activate CI** (`ci/ci.yml`) — needs a `workflow`-scoped PAT. |
| Med/Low: contracts unenforced; no `/ws` integration test | Add a TestClient websocket contract test when CI + `httpx` are in place (also resolves MPS-thread care). |
| Med: characterization goldens are bare tuples | Acceptable — there's a header naming the fields + recapture script; they're a drift alarm, paired with the invariant tests for diagnosis. |
| Low: Controls live-knob resync is structural-only | Single-user local; multi-tab live-knob sync is low value now. |
| Low: paused vs running metric window differs | Minor UX; acceptable. |
| Low: older review files mention now-fixed issues | History; this doc + the other response doc are the current status of record. |
